<div class="container">
    <div class="row">
        <div class="col-sm-12">
        {{#each articles}}
            <a href="/{{slug}}"><h1>{{title}}</h1></a>
        {{/each}}
        </div>
    </div>
</div>